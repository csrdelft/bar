export type Token = {
  tokenType?: string;
  accessToken?: string;
  refreshToken?: string;
  expiresIn?: string;
  state?: string;
};

export type LocatieToken = {
  naam?: string;
  sleutel?: string;
};

