export default defineNuxtRouteMiddleware((to, from) => {
  // FIXME:
  // const { getToken } = useToken();
  // const token = getToken();
  // if (!token) {
  //   navigateTo({ path: "/" });
  //   return;
  // }
  return;
});

